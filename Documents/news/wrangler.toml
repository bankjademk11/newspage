name = "news-app"
compatibility_date = "2024-01-01"

[env.production]
name = "news-app"

[env.preview]
name = "news-app-preview"

# Build configuration
[build]
command = "npm run build"
cwd = "."
watch_dir = "src"

# Environment variables (to be set in Cloudflare dashboard)
# [vars]
# VITE_NEWS_API_KEY = "your-api-key-here"
# VITE_API_BASE_URL = "https://newsapi.org"
