# 🚀 คู่มือการใช้ Tooltip Auto-Generator

## 🎯 ภาพรวม
`TooltipAutoGenerator` เป็นสคริปสร้างระบบ Tooltip อัตโนมัติทั้งหมด! ไม่ต้องสร้าง UI ด้วยมือเลย

## 📋 สิ่งที่สคริปทำให้:
- ✅ สร้าง ItemTooltip prefab พร้อม Text ทั้งหมด
- ✅ จัด Layout อัตโนมัติ (Vertical Layout, Content Size Fitter)
- ✅ สร้าง TooltipManager GameObject
- ✅ เชื่อมกับ InventorySlot ทั้งหมดในฉาก
- ✅ ตั้งค่าสีตามความหายาก
- ✅ บันทึก prefab อัตโนมัติ

## 🔧 วิธีใช้งาน (ง่ายมาก!)

### **ขั้นตอนที่ 1: เตรียม Canvas**
1. ตรวจสอบว่ามี Canvas ในฉากแล้ว
2. ถ้ายังไม่มี → UI → Canvas

### **ขั้นตอนที่ 2: สร้าง TooltipAutoGenerator**
1. สร้าง GameObject ว่างในฉาก
2. ตั้งชื่อว่า `TooltipAutoGenerator`
3. Add Component → `Tooltip Auto Generator`

### **ขั้นตอนที่ 3: ตั้งค่า**
- **Target Canvas**: ลาก Canvas มาใส่
- **Prefab Path**: ให้เป็น `Prefabs/ItemTooltip` (default ก็ได้)
- **สีตามความหายาก**: ปรับได้ตามต้องการ

### **ขั้นตอนที่ 4: สร้างระบบ**
1. คลิกขวาที่ Component `Tooltip Auto Generator`
2. เลือก **"Generate Complete Tooltip System"**
3. ✅ **เสร็จเลย!** ระบบ tooltip พร้อมใช้งาน

## 🎮 วิธีทดสอบ

### **ทดสอบอัตโนมัติ:**
1. คลิกขวาที่ `Tooltip Auto Generator`
2. เลือก **"Test Tooltip System"**
3. Tooltip ทดสอบจะปรากฏ

### **ทดสอบจริง:**
1. รันเกม
2. ชี้เมาส์ค้างบนช่องที่มีไอเท็ม
3. Tooltip ควรปรากฏหลัง 0.5 วินาที

## 🧹 การทำความสะอาด

### **ถ้าอยากสร้างใหม่:**
1. คลิกขวาที่ `Tooltip Auto Generator`
2. เลือก **"Clean Up Tooltip System"**
3. จะลบระบบเก่าทั้งหมด
4. สร้างใหม่ได้เลย

## 📁 ไฟล์ที่สร้างอัตโนมัติ:

### **Prefabs:**
- `Assets/Prefabs/ItemTooltip.prefab` - Tooltip UI ที่สร้างเสร็จ

### **GameObject ในฉาก:**
- `TooltipManager` - จัดการการแสดง tooltip

## 🎨 สิ่งที่ปรับแต่งได้:

### **ใน TooltipAutoGenerator:**
- **สีตามความหายาก**: ปรับสี Common, Uncommon, Rare, Epic, Legendary
- **Prefab Path**: เปลี่ยนที่เก็บ prefab ได้

### **ใน TooltipManager (หลังสร้าง):**
- **Show Delay**: รอเวลาก่อนแสดง (default 0.5 วินาที)
- **Hide Delay**: รอเวลาก่อนซ่อน (default 0.1 วินาที)
- **Enable Debug Log**: เปิด/ปิด log

## ⚡ ฟีเจอร์พิเศษ:

### **Auto-Connect:**
- สคริปจะหา InventorySlot ทั้งหมดในฉาก
- เพิ่ม EventTrigger ให้อัตโนมัติ
- เชื่อม tooltip เข้ากับทุกช่อง

### **Auto-Folder:**
- สร้างโฟลเดอร์ `Prefabs` อัตโนมัติถ้ายังไม่มี
- บันทึก prefab ในที่ที่กำหนด

### **Smart Layout:**
- ใช้ Vertical Layout Group จัดข้อความเรียงกัน
- Content Size Fitter ปรับขนาดอัตโนมัติ
- ไม่ต้องจัดตำแหน่งข้อความเอง

## 🔍 การตรวจสอบหลังสร้าง:

### **ตรวจสอบ Prefab:**
1. ไปที่ `Assets/Prefabs/ItemTooltip`
2. คลิกดูว่ามี Text components ครบหรือไม่
3. ตรวจสอบว่ามี ItemTooltip component

### **ตรวจสอบ Manager:**
1. หา GameObject `TooltipManager` ในฉาก
2. ตรวจสอบว่า Tooltip Prefab ถูกลากมาใส่
3. ตรวจสอบว่า Parent Canvas ถูกตั้งค่า

## ⚠️ ข้อควรระวัง:

1. **Canvas ต้องมีก่อน**: ต้องมี Canvas ในฉากก่อนสร้าง
2. **TextMeshPro**: ต้องติดตั้ง TextMeshPro package (Unity 2022+ มีให้)
3. **InventorySlot**: ต้องมี InventorySlot ในฉากก่อนจะเชื่อมได้
4. **One Time Only**: ไม่ควรสร้างซ้ำหลายครั้ง (ให้ Clean Up ก่อน)

## 🎯 ผลลัพธ์ที่ได้:

เมื่อสร้างเสร็จ:
- **Tooltip พร้อมใช้**: ชี้เมาส์ค้างแล้วแสดงข้อมูล
- **Auto-Connect**: ทุกช่อง inventory มี tooltip
- **Professional Look**: สวยงาม มีสีตาม rarity
- **No Manual Work**: ไม่ต้องสร้าง UI ด้วยมือเลย

---

**🎉 เสร็จสิ้น! ตอนนี้คุณมีระบบ Tooltip อัตโนมัติแล้ว!** แค่ 3 ขั้นตอนง่ายๆ และระบบพร้อมใช้งานทันที!
