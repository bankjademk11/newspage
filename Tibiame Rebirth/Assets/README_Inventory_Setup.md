# 🎮 คู่มือตั้งค่าระบบ Inventory

## ✅ ไฟล์ที่สร้างเสร็จแล้ว:
1. `Scripts/Inventory/ItemData.cs` - ScriptableObject สำหรับข้อมูลไอเท็ม
2. `Scripts/Inventory/InventorySlot.cs` - ควบคุมแต่ละช่องใน inventory
3. `Scripts/Inventory/InventoryManager.cs` - จัดการการใส่ไอเท็ม
4. `Scripts/UIGenarate/ShortInventoryGenerator.cs` - อัปเดตแล้ว (เพิ่ม Button + InventorySlot)

## 🔧 วิธีการตั้งค่าใน Unity:

### ขั้นที่ 1: สร้าง HealthPotion Asset
1. คลิกขวาที่โฟลเดอร์ `Items` → Create → Inventory → ItemData
2. ตั้งชื่อว่า `HealthPotion`
3. ใส่ค่า:
   - **Item Name**: `Health Potion`
   - **Icon**: ลากรูป `ຮinventoryBOX.png` มาใส่
   - **Is Consumable**: ✅ เช็ค
   - **Heal Amount**: `50`

### ขั้นที่ 2: ตั้งค่า Inventory Generator
1. สร้าง GameObject ว่างใน Scene ตั้งชื่อ `InventoryGenerator`
2. ใส่สคริปต์ `ShortInventoryGenerator`
3. ลาก `Canvas` มาใส่ในช่อง `Target Canvas`
4. ลากรูป `ຮinventoryBOX.png` มาใส่ในช่อง `Slot Sprite` (ถ้าต้องการ)
5. คลิกขวาที่ component → Generate UI

### ขั้นที่ 3: ตั้งค่า Inventory Manager
1. สร้าง GameObject ว่างใน Scene ตั้งชื่อ `InventoryManager`
2. ใส่สคริปต์ `InventoryManager`
3. ลาก `HealthPotion` asset มาใส่ใน `Test Items` array
4. ลากแต่ละช่องจาก `ShortInventoryUI` มาใส่ใน `Slots` array ตามลำดับ:
   - Slot_0 → ลาก Slot_1 มาใส่
   - Slot_1 → ลาก Slot_2 มาใส่
   - Slot_2 → ลาก Slot_3 มาใส่
   - ... (ทำต่อจนถึง Slot_8)

### ขั้นที่ 4: ทดสอบ
1. กด Play ▶️
2. คลิกที่ช่องที่มี Health Potion
3. ดู Console ควรขึ้น:
   - `ใช้ไอเท็ม: Health Potion`
   - `ฟื้น HP: 50`
4. ไอเท็มควรหายไป (เพราะเป็น consumable)

## 🎯 ผลลัพธ์ที่ได้:
- ✅ UI Inventory 3×3 ที่คลิกได้
- ✅ แสดงไอคอนไอเท็ม
- ✅ คลิกใช้ไอเท็มได้
- ✅ Debug Log แสดงผล
- ✅ ไอเท็ม consumable หายหลังใช้
- ✅ **ระบบเก็บไอเท็มจาก Prefab (กด E)**

## 🆕 ระบบเก็บไอเท็มจาก Scene:

### ขั้นที่ 5: ตั้งค่าระบบเก็บไอเท็ม
1. **สร้าง Prefab ไอเท็ม:**
   - สร้าง GameObject ว่าง → ตั้งชื่อ `HealthPotionPickup`
   - เพิ่ม `SpriteRenderer` → ลากรูปไอเท็มมาใส่
   - เพิ่ม `BoxCollider2D`
   - เพิ่ม `ItemPickup` script
   - ลาก `Potion` asset มาใส่ใน `Item Data`
   - ลาก `Player` GameObject มาใส่ใน `Player`
   - ลาก `InventoryManager` มาใส่ใน `Inventory Manager`
   - ลากลงโฟลเดอร์ `Prefabs` เพื่อสร้าง Prefab

2. **วางไอเท็มใน Scene:**
   - ลาก `HealthPotionPickup` prefab มาวางใน Scene
   - วางไว้ใกล้ๆ ที่ Player จะเดินผ่าน

3. **ทดสอบระบบเก็บ:**
   - กด Play ▶️
   - เดิน Player ไปใกล้ไอเท็ม (ระยะ ≤ 2 หน่วย)
   - กดปุ่ม `E`
   - ดู Console ควรขึ้น:
     - `เก็บไอเท็ม: Health Potion`
     - `เพิ่ม Health Potion ลงช่องที่ X`
   - ไอเท็มใน Scene ควรหายไป
   - ไอเท็มควรปรากฎในช่องว่างใน inventory

## 🔄 ถ้ามีปัญหา:
- **เก็บไอเท็มไม่ได้:** ตรวจสอบว่า Player มี Tag "Player" หรือไม่
- **ไม่เจอ Player:** ลาก Player GameObject มาใส่ใน ItemPickup component
- **กระเป๋าเต็ม:** ตรวจสอบว่ามีช่องว่างใน inventory
- **ไม่มี EventSystem:** Unity สร้างให้อัตโนมัติเมื่อสร้าง UI
- **Canvas ไม่ทำงาน:** ตรวจสอบว่ามี `Graphic Raycaster` component
