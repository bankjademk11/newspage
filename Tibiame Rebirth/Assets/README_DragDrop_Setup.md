# 🎮 คู่มือตั้งค่าระบบ Drag & Drop สำหรับ Inventory

## ✅ ไฟล์ที่สร้าง/อัปเดต:
1. `Scripts/Inventory/DragDropHandler.cs` - จัดการการลากวางแต่ละช่อง
2. `Scripts/Inventory/DragDropManager.cs` - จัดการระบบ Drag & Drop รวม
3. `Scripts/Inventory/MainInventoryGenerator.cs` - อัปเดตเพิ่ม DragDrop
4. `Scripts/UIGenarate/ShortInventoryGenerator.cs` - อัปเดตเพิ่ม DragDrop

## 🎯 ฟีเจอร์ที่ได้:
- ✅ **ลากไอเท็ม** ภายใน MainInventory (20 ช่อง)
- ✅ **ลากไอเท็ม** ภายใน ShortInventory (9 ช่อง)
- ✅ **ลากข้ามระบบ** MainInventory ↔ ShortInventory
- ✅ **สลับไอเท็ม** ถ้าช่องเป้าหมายมีไอเท็มอยู่แล้ว
- ✅ **Visual Feedback** แสดงไอเท็มที่กำลังลาก
- ✅ **Debug Log** แสดงการสลับไอเท็ม
- ✅ **Auto Registration** ลงทะเบียนช่องอัตโนมัติ

## 🔧 วิธีการตั้งค่าใน Unity:

### ขั้นที่ 1: สร้าง DragDropManager
1. **สร้าง GameObject** ว่าง → ตั้งชื่อ `DragDropManager`
2. **ใส่สคริปต์** `DragDropManager`
3. **ตั้งค่า** (ถ้าต้องการ):
   - `Enable Debug Log`: ✅ (แสดง log)
   - `Enable Sound Effects`: ❌ (ปิดเสียงไว้ก่อน)
   - `Allow Same Type Stack`: ❌ (ไม่ซ้อนไอเท็ม)

### ขั้นที่ 2: สร้าง/อัปเดต UI
**ถ้ามี UI อยู่แล้ว:**
- ลบ UI เก่า แล้วสร้างใหม่เพื่อให้มี DragDrop

**สร้าง MainInventory:**
1. **สร้าง GameObject** → `MainInventoryGenerator`
2. **ใส่สคริปต์** `MainInventoryGenerator`
3. **ลาก Canvas** มาใส่
4. **คลิก Generate UI** → สร้าง 20 ช่อง + DragDrop

**สร้าง ShortInventory:**
1. **สร้าง GameObject** → `ShortInventoryGenerator`
2. **ใส่สคริปต์** `ShortInventoryGenerator`
3. **ลาก Canvas** มาใส่
4. **คลิก Generate UI** → สร้าง 9 ช่อง + DragDrop

### ขั้นที่ 3: ตั้งค่า InventoryManager
1. **สร้าง GameObject** → `InventoryManager`
2. **ใส่สคริปต์** `InventoryManager`
3. **ลากช่องทั้งหมด** มาใส่ใน `Slots` array:
   - 20 ช่องจาก MainInventoryUI
   - 9 ช่องจาก ShortInventoryUI
4. **เพิ่มไอเท็มตัวอย่าง** ใน `Test Items`

### ขั้นที่ 4: ทดสอบระบบ
1. **กด Play ▶️**
2. **ใส่ไอเท็ม** ในช่อง (ผ่าน Inspector หรือเก็บจาก Scene)
3. **ทดสอบลาก**:
   - ลากภายใน MainInventory
   - ลากภายใน ShortInventory
   - ลากข้ามระบบ Main ↔ Short
4. **ดู Console** ควรขึ้น:
   - `เริ่มลาก: [ชื่อไอเท็ม]`
   - `สลับไอเท็มสำเร็จ: [ชื่อ1] ↔ [ชื่อ2]`

## 🎮 การใช้งานจริง:
- **ลากไอเท็ม** → คลิกแล้วลากไปยังช่องอื่น
- **สลับไอเท็ม** → ลากไปช่องที่มีไอเท็มอยู่แล้ว
- **ย้ายข้ามระบบ** → ลากระหว่าง Main และ Short inventory
- **คลิกใช้ไอเท็ม** → คลิกปกติ (ยังทำงานเหมือนเดิม)

## 🔧 การปรับแต่ง DragDropHandler:
แต่ละช่องมี `DragDropHandler` สามารถปรับได้:
- `Dragged Alpha`: ความโปร่งของไอเท็มที่ลาก (0.8)
- `Dragged Scale`: ขนาดของไอเท็มที่ลาก (1.2)
- `Canvas`: Canvas ที่ใช้ (หาอัตโนมัติ)

## 🔄 ถ้ามีปัญหา:
- **ลากไม่ได้**: ตรวจสอบว่ามี `DragDropManager` ใน Scene หรือไม่
- **ไม่มี EventSystem**: Unity สร้างให้อัตโนมัติเมื่อสร้าง UI
- **ลากข้ามระบบไม่ได้**: ตรวจสอบว่าลาก Canvas มาใส่ใน Generator ครบหรือไม่
- **ช่องไม่ลงทะเบียน**: สร้าง UI ใหม่ผ่าน Generator

## 🎯 ขั้นตอนสรุป:
1. ✅ สร้าง `DragDropManager` GameObject
2. ✅ สร้าง `MainInventoryGenerator` → Generate UI
3. ✅ สร้าง `ShortInventoryGenerator` → Generate UI
4. ✅ สร้าง `InventoryManager` → ลากช่องทั้งหมดมาใส่
5. ✅ เพิ่มไอเท็มตัวอย่าง
6. ✅ ทดสอบลากวาง

**ระบบ Drag & Drop พร้อมใช้งานแล้ว! 🚀**
- ลากไอเท็มระหว่าง MainInventory (20 ช่อง) และ ShortInventory (9 ช่อง)
- สลับไอเท็มได้ทุกช่อง
- มี Visual Feedback และ Debug Log ครบครัน
