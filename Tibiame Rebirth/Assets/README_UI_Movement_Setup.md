# UI Movement Block Setup Guide

## ภาพรวม
การตั้งค่าระบบป้องกันการเคลื่อนที่ของ Player เมื่อเปิด UI และการปรับระบบการหันหน้าตามทิศทาง A/D

## ไฟล์ที่เพิ่ม/แก้ไข

### 1. UIManager.cs (ไฟล์ใหม่)
- **จุดประสงค์**: จัดการสถานะ UI ทั้งหมดในเกม
- **ฟีเจอร์หลัก**:
  - ตรวจสอบว่ามี UI ใดเปิดอยู่หรือไม่ (`IsAnyUIOpen()`)
  - หา UI components อัตโนมัติ
  - ปิด UI ทั้งหมดในกรณีฉุกเฉิน
- **Singleton Pattern**: ใช้รูปแบบ Singleton เพื่อให้สามารถเรียกใช้จากที่ไหนก็ได้

### 2. PlayerController.cs (แก้ไข)
- **การเคลื่อนที่**: ตรวจสอบสถานะ UI ก่อนอนุญาตการเคลื่อนที่
- **การหันหน้า**: เปลี่ยนจากการตามเมาส์เป็นการตามทิศทาง A/D เท่านั้น
- **Animation**: หยุด animation เมื่อมี UI เปิดอยู่

## วิธีการตั้งค่า

### ขั้นตอนที่ 1: เพิ่ม UIManager ในฉาก
1. สร้าง GameObject ใหม่ในฉาก
2. ตั้งชื่อว่า "UIManager"
3. เพิ่ม component `UIManager.cs` เข้าไป
4. UIManager จะหา UI components อัตโนมัติ (InventoryToggle, EquipmentToggle, ShopToggle)

### ขั้นตอนที่ 2: ตรวจสอบการเชื่อมต่อ
- **PlayerController**: จะหา UIManager อัตโนมัติเมื่อเริ่มเกม
- **UI Components**: UIManager จะหา Toggle scripts อัตโนมัติ

### ขั้นตอนที่ 3: ทดสอบการทำงาน
1. **ทดสอบการป้องกันการเคลื่อนที่**:
   - กดปุ่ม I เพื่อเปิด Inventory → Player ไม่สามารถเคลื่อนที่ได้
   - ปิด Inventory → Player สามารถเคลื่อนที่ได้
   - ทดสอบกับ Equipment และ Shop ด้วย

2. **ทดสอบการหันหน้า**:
   - กด A → Player หันหน้าซ้าย
   - กด D → Player หันหน้าขวา
   - กด W/S → Player เดินขึ้น/ลงแต่ยังหันหน้าทิศทางเดิม

## ฟีเจอร์ที่ได้รับ

### ✅ ป้องกันการเคลื่อนที่เมื่อเปิด UI
- Player ไม่สามารถเคลื่อนที่ได้เมื่อเปิด Inventory, Equipment, หรือ Shop
- Animation หยุดทำงานเมื่อมี UI เปิดอยู่
- Player กลับมาเคลื่อนที่ได้ทันทีเมื่อปิด UI

### ✅ การหันหน้าตามทิศทาง A/D
- ลบการหันหน้าตามตำแหน่งเมาส์
- กด A = หันหน้าซ้าย, กด D = หันหน้าขวา
- กด W/S = เดินขึ้น/ลงแต่ไม่เปลี่ยนทิศทางการหันหน้า

## การแก้ไขปัญหา

### ปัญหา: Player ยังเคลื่อนที่ได้เมื่อเปิด UI
- ตรวจสอบว่ามี UIManager ในฉากหรือไม่
- ตรวจสอบว่า UI Toggle scripts ถูกเชื่อมต่อกับ UIManager หรือไม่
- ดู Console log สำหรับข้อความแจ้งเตือน

### ปัญหา: Player ไม่หันหน้าตาม A/D
- ตรวจสอบว่า SpriteRenderer มีอยู่ใน Player หรือไม่
- ตรวจสอบว่าการกดปุ่ม A/D ให้ค่า movement.x ที่ถูกต้องหรือไม่

### ปัญหา: Animation ไม่ทำงาน
- ตรวจสอบว่ามี Animator component ใน Player หรือไม่
- ตรวจสอบว่ามี parameter "isWalk" ใน Animator หรือไม่

## ฟีเจอร์เพิ่มเติม: Camera Controller

### 📷 CameraController.cs
- **จุดประสงค์**: ควบคุมกล้องให้ติดตาม Player แบบ Tibiame
- **ฟีเจอร์หลัก**:
  - ติดตาม Player อย่างราบรื่นด้วย SmoothDamp
  - หา Player อัตโนมัติจาก Tag "Player"
  - สามารถตั้งค่าขอบเขตของกล้องได้
  - ปรับค่าความเร็วในการติดตามได้

### วิธีการตั้งค่า Camera Controller:
1. **เพิ่ม Camera Controller**:
   - เลือก Main Camera ในฉาก
   - เพิ่ม component `CameraController.cs` เข้าไป
   - กล้องจะหา Player อัตโนมัติจาก Tag "Player"

2. **การตั้งค่าเพิ่มเติม**:
   - **Smooth Speed**: ปรับความราบรื่นในการติดตาม (ค่าเริ่มต้น = 5)
   - **Offset**: ปรับระยะห่างจาก Player (Z=-10 สำหรับ 2D)
   - **Bounds**: ตั้งค่าขอบเขตของกล้องถ้าต้องการจำกัดพื้นที่

3. **การตั้งค่า Player Tag**:
   - ตรวจสอบให้แน่ใจว่า Player GameObject มี Tag = "Player"
   - ถ้าไม่มี ให้สร้าง Tag "Player" และกำหนดให้ Player

## ข้อควรรู้
- UIManager ใช้ Singleton pattern จึงมีเพียง 1 ตัวต่อฉาก
- ระบบจะหา components อัตโนมัติ ไม่ต้องลากวาง manually
- สามารถเพิ่ม UI ชนิดอื่นๆ ได้โดยการแก้ไข UIManager
- Camera Controller จะหา Player อัตโนมัติจาก Tag "Player"
