# 🛒 Shop System Setup Guide

## ภาพรวมระบบร้านค้า
ระบบร้านค้าอัตโนมัติสำหรับ Unity ที่ช่วยให้คุณสร้าง UI ร้านค้าได้ง่ายและรวดเร็ว

## 📁 ไฟล์ที่ต้องการ
- `Scripts/Shop/ShopItemData.cs` - ScriptableObject สำหรับเก็บข้อมูลไอเทมร้านค้า
- `Scripts/Shop/ShopSlot.cs` - คอนโทรล UI แต่ละช่องในร้านค้า
- `Scripts/Shop/ShopGenerator.cs` - เครื่องมือสร้าง UI ร้านค้าอัตโนมัติ

## 🚀 วิธีการตั้งค่า (Step-by-step)

### 1. สร้าง Canvas
```
GameObject -> UI -> Canvas
```

### 2. สร้าง ShopManager GameObject
```
- สร้าง GameObject เปล่าๆ 
- ตั้งชื่อว่า "ShopManager"
- ใส่สคริปต์ ShopGenerator ลงไป
```

### 3. ตั้งค่า ShopGenerator
ใน Inspector ของ ShopGenerator:
- **Target Canvas**: ลาก Canvas ของ Scene มาใส่
- **Layout**: ปรับค่าตามต้องการ
  - `Columns`: จำนวนคอลัมน์ (default: 5)
  - `Rows`: จำนวนแถว (default: 4)
  - `Slot Size`: ขนาดช่อง (default: 80x80)
  - `Slot Spacing`: ระยะห่างช่อง (default: 8)
- **Styling**: ปรับแต่งสีและฟอนต์
- **Names**: ตั้งชื่อร้านค้า (default: "Shop")

### 4. สร้าง UI ร้านค้า
```
คลิกขวาที่ชื่อสคริปต์ ShopGenerator ใน Inspector 
→ เลือก "Generate Shop UI"
```

### 5. สร้างข้อมูลไอเทม
```
Right click ใน Project window 
→ Create → Shop → ShopItemData
```
ตั้งค่าข้อมูลไอเทม:
- `Item Name`: ชื่อไอเทม
- `Icon`: รูปภาพไอเทม
- `Price`: ราคา
- `Stackable`: สามารถซ้อนกันได้หรือไม่

## 🎮 การใช้งาน

### การเปิด/ปิดร้านค้า
```csharp
// หา GameObject ของร้านค้า
GameObject shopUI = GameObject.Find("AutoShopUI");

// เปิดร้านค้า
shopUI.SetActive(true);

// ปิดร้านค้า
shopUI.SetActive(false);
```

### การเพิ่มไอเทมในร้านค้า
```csharp
// หา ShopSlot ทั้งหมด
ShopSlot[] slots = FindObjectsOfType<ShopSlot>();

// สร้างข้อมูลไอเทม
ShopItemData item = Resources.Load<ShopItemData>("ShopItems/HealthPotion");

// ใส่ไอเทมในช่องแรก
if (item != null && slots.Length > 0)
{
    slots[0].SetItem(item);
}
```

### การเชื่อมต่อกับระบบอื่นๆ
ใน `ShopSlot.cs` แก้ไขฟังก์ชัน `OnBuyClicked()`:

```csharp
public void OnBuyClicked()
{
    if (itemData == null) return;
    
    // ตรวจสอบเงินของผู้เล่น
    PlayerStats playerStats = FindObjectOfType<PlayerStats>();
    if (playerStats.gold >= itemData.price)
    {
        // หักเงิน
        playerStats.gold -= itemData.price;
        
        // เพิ่มไอเทมใน Inventory
        InventoryManager inventory = FindObjectOfType<InventoryManager>();
        inventory.AddItem(itemData.itemName, 1);
        
        Debug.Log($"ซื้อ {itemData.itemName} สำเร็จ!");
    }
    else
    {
        Debug.Log("เงินไม่พอ!");
    }
}
```

## 🎨 การปรับแต่ง UI

### เปลี่ยนสีพื้นหลัง
แก้ไขใน `ShopGenerator.cs`:
```csharp
bg.color = new Color(0.08f, 0.06f, 0.04f, 0.95f); // สีเข้มแบบร้าน
```

### เปลี่ยนสีช่อง
```csharp
slotImg.color = new Color(0.35f, 0.25f, 0.18f); // สีหนัง
```

### เปลี่ยนฟอนต์
- ลาก Font มาใส่ในช่อง `UI Font` ของ ShopGenerator
- หรือใช้ฟอนต์ที่มีในเกมอยู่แล้ว

## 🔧 การตั้งค่าขั้นสูง

### การสร้างหลายร้านค้า
```csharp
// สร้าง ShopGenerator หลายตัว
ShopGenerator weaponShop = gameObject.AddComponent<ShopGenerator>();
ShopGenerator potionShop = gameObject.AddComponent<ShopGenerator>();

// ตั้งค่าแยกกัน
weaponShop.shopTitle = "Weapon Shop";
potionShop.shopTitle = "Potion Shop";
```

### การบันทึก/โหลดข้อมูลร้านค้า
```csharp
[System.Serializable]
public class ShopData
{
    public string shopName;
    public ShopItemData[] items;
    public Vector2Int gridSize;
}
```

## 🐛 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: UI ไม่แสดง
- ตรวจสอบว่า Canvas ถูกลากมาใส่ใน Target Canvas หรือไม่
- ตรวจสอบว่า Canvas มี Canvas Scaler หรือไม่

### ปัญหา: ปุ่มไม่ทำงาน
- ตรวจสอบว่ามี Graphic Raycaster ใน Canvas หรือไม่
- ตรวจสอบว่า EventSystem มีใน Scene หรือไม่

### ปัญหา: ไอเทมไม่แสดง
- ตรวจสอบว่า ShopItemData ถูกสร้างและตั้งค่าถูกต้อง
- ตรวจสอบว่ามีการเรียก SetItem() หรือไม่

## 📝 หมายเหตุ
- ระบบนี้ออกแบบมาให้ทำงานใน Unity Editor เท่านั้น (ใช้ `#if UNITY_EDITOR`)
- สามารถปรับแต่งเพิ่มเติมได้ตามความต้องการของเกม
- แนะนำให้สร้าง Prefab ของร้านค้าเพื่อใช้ซ้ำ

## 🎯 ถัดไป
- สร้างระบบจัดการเงิน/คะแนน
- เชื่อมต่อกับระบบ Inventory
- เพิ่มเอฟเฟกต์เสียงและภาพเคลื่อนไหว
- สร้างระบบ discount หรือ promotion
