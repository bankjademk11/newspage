# 🛒 Shop Auto System Setup Guide

## ภาพรวมระบบร้านค้าอัตโนมัติ
ระบบร้านค้าอัตโนมัติที่ทำงานเหมือนระบบ Equipment สามารถติดตั้งได้ด้วยคลิกเดียวผ่าน Unity Menu

## 📁 ไฟล์ที่ต้องการ
- `Scripts/Shop/ShopItemData.cs` - ScriptableObject สำหรับเก็บข้อมูลไอเทมร้านค้า
- `Scripts/Shop/ShopSlot.cs` - คอนโทรล UI แต่ละช่องในร้านค้า
- `Scripts/Shop/ShopGenerator.cs` - เครื่องมือสร้าง UI ร้านค้า
- `Scripts/Shop/ShopManager.cs` - จัดการการซื้อขายและเงิน
- `Scripts/UIGenarate/ShopAutoGenerator.cs` - เครื่องมือติดตั้งระบบอัตโนมัติ

## 🚀 วิธีการติดตั้ง (Auto Setup)

### 1. ติดตั้งระบบร้านค้า
```
Unity Menu → Tools → Shop System → Setup Shop System
```

ระบบจะทำการ:
- ✅ สร้าง Canvas พร้อม Component ที่จำเป็น (Canvas Scaler, Graphic Raycaster, Event System)
- ✅ สร้าง Shop UI พร้อมช่องว่าง 20 ช่อง (5x4)
- ✅ สร้าง Shop Toggle Button สำหรับเปิด/ปิดร้านค้า
- ✅ สร้าง Shop Manager สำหรับจัดการการซื้อขาย
- ✅ ตั้งค่า CanvasGroup สำหรับการ fade in/out

### 2. สร้างไอเท็มตัวอย่าง
```
Unity Menu → Tools → Shop System → Create Sample Shop Items
```

จะสร้างไอเท็มตัวอย่าง:
- Health Potion (50 gold)
- Mana Potion (75 gold)
- Iron Sword (200 gold)
- Iron Armor (350 gold)
- Magic Ring (500 gold)

### 3. ลบระบบ (ถ้าต้องการ)
```
Unity Menu → Tools → Shop System → Destroy Shop System
```

## 🎮 การใช้งาน

### เปิด/ปิดร้านค้า
- กดปุ่ม "Shop" ที่มุมขวาบนของหน้าจอ
- หรือใช้โค้ด:
```csharp
// หา Shop Panel
Transform shopPanel = GameObject.Find("Canvas").transform.Find("AutoShopUI");
CanvasGroup canvasGroup = shopPanel.GetComponent<CanvasGroup>();

// เปิดร้านค้า
canvasGroup.alpha = 1;
canvasGroup.interactable = true;
canvasGroup.blocksRaycasts = true;

// ปิดร้านค้า
canvasGroup.alpha = 0;
canvasGroup.interactable = false;
canvasGroup.blocksRaycasts = false;
```

### การซื้อของ
- คลิกที่ไอเท็มในร้านค้าเพื่อซื้อ
- ระบบจะตรวจสอบเงินและเพิ่มไอเท็มใน Inventory อัตโนมัติ

### การจัดการเงิน
```csharp
ShopManager shopManager = FindObjectOfType<ShopManager>();

// ดูเงินปัจจุบัน
int gold = shopManager.GetPlayerGold();

// เพิ่มเงิน
shopManager.AddGold(100);

// ตั้งค่าเงิน
shopManager.SetPlayerGold(5000);
```

## 🎨 การปรับแต่ง

### เปลี่ยนขนาดร้านค้า
แก้ไขใน `ShopGenerator.cs`:
```csharp
public int columns = 5;  // จำนวนคอลัมน์
public int rows = 4;     // จำนวนแถว
public Vector2 slotSize = new Vector2(80, 80); // ขนาดช่อง
```

### เปลี่ยนสีร้านค้า
```csharp
bg.color = new Color(0.08f, 0.06, 0.04, 0.95f); // สีพื้นหลัง
buttonImage.color = new Color(0.1f, 0.4f, 0.1f, 0.8f); // สีปุ่ม Shop
```

### เปลี่ยนตำแหน่งปุ่ม Shop
ใน `ShopAutoGenerator.cs`:
```csharp
toggleRect.anchoredPosition = new Vector2(-70, -80); // ปรับตำแหน่ง
```

## 🔧 การเชื่อมต่อกับระบบอื่น

### เชื่อมกับ PlayerStats
แก้ไข `ShopManager.cs`:
```csharp
public bool TryPurchaseItem(ShopItemData item)
{
    PlayerStats playerStats = FindObjectOfType<PlayerStats>();
    if (playerStats.gold >= item.price)
    {
        playerStats.gold -= item.price;
        // ... เพิ่มไอเท็มใน Inventory
        return true;
    }
    return false;
}
```

### เชื่อมกับระบบ Equipment
```csharp
if (item.itemType == ItemType.Weapon || item.itemType == ItemType.Armor)
{
    // สร้างไอเท็มสำหรับระบบ Equipment
    ItemData equipmentItem = CreateEquipmentItem(item);
    inventory.AddItem(equipmentItem);
}
```

## 🐛 การแก้ไขปัญหา

### ปัญหา: ไม่เห็นปุ่ม Shop
- ตรวจสอบว่ามี Canvas ในฉากหรือไม่
- ลอง Setup Shop System ใหม่

### ปัญหา: กดปุ่มไม่ทำงาน
- ตรวจสอบว่ามี EventSystem ในฉากหรือไม่
- ตรวจสอบว่ามี Graphic Raycaster ใน Canvas หรือไม่

### ปัญหา: ซื้อของไม่ได้
- ตรวจสอบว่ามี ShopManager ในฉากหรือไม่
- ตรวจสอบว่ามี InventoryManager ในฉากหรือไม่
- ตรวจสอบว่ามีไอเท็มในร้านค้าหรือไม่

## 📝 หมายเหตุ
- ระบบนี้ทำงานเหมือน Equipment System ทุกประการ
- ใช้ CanvasGroup สำหรับการ fade in/out แทน SetActive
- สามารถทำงานร่วมกับระบบอื่นๆ ได้
- แนะนำให้ใช้ Auto Setup แทนการสร้างแบบ manual

## 🎯 ถัดไป
- เพิ่มเอฟเฟกต์เสียงและภาพเคลื่อนไหว
- สร้างระบบ discount และ promotion
- เพิ่มระบบคลังสินค้า (stock system)
- สร้างร้านค้าหลายประเภท (weapon shop, potion shop ฯลฯ)
