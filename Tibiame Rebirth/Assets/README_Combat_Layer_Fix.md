# 🛠️ Combat System Layer Fix

## 🐛 **ปัญหาที่พบ:**
เมื่อใช้ Combat Auto Generator อาจเกิด NullReferenceException เนื่องจากไม่พบ Layer "Enemy" ใน Unity Project

## 🔧 **วิธีแก้ไข (แบบถาวร):**

### 1. **สร้าง Layer "Enemy" ใน Unity**
1. เปิด Unity Editor
2. ไปที่ `Edit > Project Settings`
3. เลือก `Tags and Layers`
4. ในส่วน `Layers` ให้เพิ่ม Layer ใหม่:
   - ในช่องว่าง (User Layer 8 หรือที่ว่าง) พิมพ์: `Enemy`
5. กด `Apply` หรือ `Save`

### 2. **ตรวจสอบ Layer**
- ตรวจสอบว่า Layer "Enemy" ปรากฏในรายการ Layers
- สามารถตรวจสอบได้จาก Inspector ของ GameObject ในส่วน Layer dropdown

## 🚀 **หลังจากสร้าง Layer:**
1. กลับไปใช้ Combat Auto Generator ได้ตามปกติ
2. Tool จะทำงานได้ถูกต้องโดยไม่มี Error
3. Enemy Prefabs จะถูกกำหนด Layer ให้ถูกต้อง

## 📋 **ขั้นตอนการใช้งาน Combat Auto Generator:**
1. เปิด Unity Editor
2. ไปที่ `Tools > UI Generator > Combat Auto Generator`
3. **ปล่อยช่อง "Combat System Parent" ว่าง** (จะสร้างใหม่อัตโนมัติ)
4. กด `🚀 Generate Combat System`
5. ระบบจะสร้าง:
   - TargetManager
   - CombatManager  
   - CombatUI
   - Enemy Prefabs
   - Enemy Data Assets

## ⚠️ **ข้อควรรู้:**
- ถ้าไม่สร้าง Layer "Enemy" Tool จะใช้ Default layer (0) และแสดง Warning
- แนะนำให้สร้าง Layer "Enemy" เพื่อการทำงานที่ถูกต้อง
- Layer สำคัญสำหรับระบบ Targeting และ Combat

## ✅ **ตรวจสอบผลลัพธ์:**
หลังจากสร้างเสร็จ คุณจะพบ:
- GameObject "CombatSystem" ใน Hierarchy
- Enemy Prefabs ใน `Assets/Prefabs/Enemies`
- Enemy Data ใน `Assets/Items/EnemyData`
- ไม่มี Error ใน Console

---
**🎯 เคล็ดลับ:** สร้าง Layer "Enemy" ครั้งเดียว จะใช้ได้ตลอดการพัฒนาโปรเจ็กต์!
