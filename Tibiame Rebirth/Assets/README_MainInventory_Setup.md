# 🎮 คู่มือตั้งค่า MainInventory พร้อมระบบเปิด/ปิด

## ✅ ไฟล์ที่สร้างเสร็จแล้ว:
1. `Scripts/Inventory/MainInventoryGenerator.cs` - สร้าง UI 20 ช่อง (5×4)
2. `Scripts/Inventory/InventoryToggle.cs` - ระบบเปิด/ปิดด้วยปุ่ม I
3. `Scripts/Inventory/InventorySlot.cs` - ควบคุมแต่ละช่อง
4. `Scripts/Inventory/ItemData.cs` - ข้อมูลไอเท็ม
5. `Scripts/Inventory/InventoryManager.cs` - จัดการการใส่ไอเท็ม
6. `Scripts/Inventory/ItemPickup.cs` - ระบบเก็บไอเท็มจาก Scene

## 🔧 วิธีการตั้งค่าใน Unity:

### ขั้นที่ 1: สร้าง MainInventory UI
1. **สร้าง Canvas** (ถ้ายังไม่มี):
   - UI → Canvas
2. **สร้าง GameObject** ว่าง → ตั้งชื่อ `MainInventoryGenerator`
3. **ใส่สคริปต์** `MainInventoryGenerator`
4. **ลาก Canvas** มาใส่ในช่อง `Target Canvas`
5. **ตั้งค่า UI** (ถ้าต้องการ):
   - `Slot Size`: 80×80 (ขนาดช่อง)
   - `Slot Spacing`: 10 (ระยะห่าง)
   - `Slot Sprite`: ลากรูปช่องมาใส่ (ถ้ามี)
6. **ตั้งค่า Toggle System**:
   - `Create Toggle System`: ✅ เช็ค
   - `Pause Game When Open`: ❌ (ถ้าไม่ต้องการหยุดเวลา)
   - `Show Cursor When Open`: ✅ (แสดงเมาส์ตอนเปิด)
7. **คลิกขวา** ที่ component → **Generate UI**

### ขั้นที่ 2: สร้างไอเท็มตัวอย่าง
1. **คลิกขวา** ที่โฟลเดอร์ `Items` → Create → Inventory → ItemData
2. **ตั้งชื่อ** `HealthPotion`
3. **ใส่ค่า**:
   - **Item Name**: `Health Potion`
   - **Icon**: ลารูปไอเท็มมาใส่
   - **Is Consumable**: ✅ เช็ค
   - **Heal Amount**: `50`

### ขั้นที่ 3: ตั้งค่า InventoryManager
1. **สร้าง GameObject** ว่าง → ตั้งชื่อ `InventoryManager`
2. **ใส่สคริปต์** `InventoryManager`
3. **ลาก `HealthPotion`** asset มาใส่ใน `Test Items` array
4. **ลากแต่ละช่อง** จาก `MainInventoryUI` มาใส่ใน `Slots` array:
   - Slot_0 → ลาก Slot_1 มาใส่
   - Slot_1 → ลาก Slot_2 มาใส่
   - ... (ทำต่อจนถึง Slot_19)

### ขั้นที่ 4: ทดสอบระบบ
1. **กด Play ▶️**
2. **กดปุ่ม I** → เปิด/ปิด MainInventory
3. **คลิกที่ช่อง** ที่มี Health Potion → ใช้ไอเท็ม
4. **ดู Console** ควรขึ้น:
   - `🎒 เปิด Inventory` / `🎒 ปิด Inventory`
   - `ใช้ไอเท็ม: Health Potion`
   - `ฟื้น HP: 50`

## 🎯 ฟีเจอร์ที่ได้:
- ✅ **UI 20 ช่อง** (5×4) พร้อมปุ่มคลิก
- ✅ **เปิด/ปิดด้วยปุ่ม I** อัตโนมัติ
- ✅ **ซ่อน UI ตอนเริ่ม** เกม
- ✅ **แสดงเมาส์** ตอนเปิด inventory
- ✅ **Debug Log** แสดงสถานะ
- ✅ **ใช้ไอเท็ม** ได้ (คลิกที่ช่อง)
- ✅ **Consumable** ไอเท็มหายหลังใช้

## 🆕 ระบบเก็บไอเท็มจาก Scene (ถ้าต้องการ):
1. **สร้าง Prefab ไอเท็ม**:
   - GameObject ว่าง → ตั้งชื่อ `HealthPotionPickup`
   - เพิ่ม `SpriteRenderer` + `BoxCollider2D` + `ItemPickup` script
   - ลาก `HealthPotion` asset มาใส่ใน `Item Data`
   - ลาก `Player` และ `InventoryManager` มาใส่
2. **วางใน Scene** และทดสอบกด **E** เพื่อเก็บ

## 🔄 ถ้ามีปัญหา:
- **กด I ไม่ได้**: ตรวจสอบว่ามี `InventoryToggle` GameObject หรือไม่
- **UI ไม่หาย**: ตรวจสอบ `Create Toggle System` ใน MainInventoryGenerator
- **ใช้ไอเท็มไม่ได้**: ตรวจสอบว่าลากช่องใส่ InventoryManager ครบหรือไม่
- **ไม่มี EventSystem**: Unity สร้างให้อัตโนมัติเมื่อสร้าง UI

## 🎮 การใช้งานจริง:
- **กด I** → เปิด/ปิด MainInventory
- **คลิกช่อง** → ใช้ไอเท็มในช่องนั้น
- **กด E** (ใกล้ไอเท็ม) → เก็บไอเท็มจาก Scene
- **Debug Log** → ดูผลการทำงานใน Console

**ระบบ MainInventory พร้อมใช้งานแล้ว! 🚀**
