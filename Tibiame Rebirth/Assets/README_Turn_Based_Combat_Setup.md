# 🔄 Turn-Based Combat System Setup

## ภาพรวมระบบ

ระบบ Turn-Based Combat สำหรับเกมแบบ Tibia ที่ออกแบบมาให้ Player และ Enemy โจมตีสลับกันตามรอบ โดย Enemy จะโจมตีก่อนเสมอ

## 🎯 ลักษณะการทำงาน

### 1. การเริ่มต้นการต่อสู้
- Player คลิกขวาที่ Enemy เพื่อเลือกเป้าหมาย
- ถ้าอยู่ในระยะเริ่มการต่อสู้ (2.0 หน่วย) แต่ไกลกว่าระยะโจมตี (1.5 หน่วย)
  - Player จะวิ่งไปหา Enemy อัตโนมัติ
- ถ้าอยู่ในระยะโจมตีแล้ว จะเริ่มการต่อสู้ทันที

### 2. ลำดับการโจมตี
1. **Enemy โจมตีก่อนเสมอ**
2. **Player โจมตีตาม** (อัตโนมัติในรอบของ Player)
3. **วนซ้ำ** จนกว่าฝ่ายใดฝ่ายหนึ่งจะตาย

### 3. การเคลื่อนที่
- Player จะวิ่งไปหา Enemy อัตโนมัติเมื่อเริ่มการต่อสู้
- ระหว่างการต่อสู้ Player ไม่สามารถเคลื่อนที่ได้ (Turn-based mode)
- Enemy จะหยุดเคลื่อนที่เมื่ออยู่ในการต่อสู้

## 🛠️ Components ที่ต้องการ

### 1. TurnManager.cs
- **หน้าที่**: จัดการลำดับการโจมตีและสถานะการต่อสู้
- **สถานะ**: None → Moving → Combat → EnemyTurn → PlayerTurn → CombatEnd
- **Events**: OnCombatStarted, OnPlayerTurnStart, OnEnemyTurnStart, OnCombatEnded

### 2. CombatManager.cs (แก้ไขแล้ว)
- **การเปลี่ยนแปลง**: ตรวจสอบ TurnManager ก่อนทำ Auto-attack
- **Turn-based mode**: ไม่ทำ Auto-attack และ Input ปกติ
- **Normal mode**: ทำงานเหมือนเดิม

### 3. EnemyController.cs (แก้ไขแล้ว)
- **การเปลี่ยนแปลง**: รอรอบการโจมตีจาก TurnManager
- **PerformAttack()**: เปลี่ยนเป็น public เพื่อให้ TurnManager เรียกได้
- **Turn-based mode**: ไม่โจมตีอัตโนมัติ รอคำสั่งจาก TurnManager

## 📋 การตั้งค่า

### 1. สร้าง TurnManager GameObject
```csharp
// สร้าง GameObject ใหม่
GameObject turnManagerObj = new GameObject("TurnManager");
TurnManager turnManager = turnManagerObj.AddComponent<TurnManager>();
```

### 2. ตั้งค่า TurnManager
- **Player**: ลาก Player GameObject มาใส่
- **Combat Manager**: ลาก CombatManager มาใส่
- **Target Manager**: ลาก TargetManager มาใส่
- **Player Controller**: ลาก PlayerController มาใส่

### 3. ตั้งค่าระยะทาง
- **Combat Start Range**: 2.0 (ระยะเริ่มการต่อสู้)
- **Attack Range**: 1.5 (ระยะโจมตี)
- **Move Speed**: 3.0 (ความเร็ววิ่งไปหาเป้าหมาย)

## 🎮 การทำงานของระบบ

### Flow Chart
```
Player Click Enemy
        ↓
Check Distance
        ↓
Distance > Attack Range?
    YES → Move to Target → Start Combat
    NO  → Start Combat
        ↓
Enemy Turn (Attack First)
        ↓
Player Turn (Auto Attack)
        ↓
Check Death
        ↓
Both Alive? → Repeat
Someone Dead? → End Combat
```

### State Management
```csharp
public enum CombatState
{
    None,           // ไม่มีการต่อสู้
    Moving,         // Player กำลังวิ่งไปหา Enemy
    Combat,         // อยู่ในการต่อสู้แบบ Turn-based
    PlayerTurn,     // รอบของ Player
    EnemyTurn,      // รอบของ Enemy
    CombatEnd       // จบการต่อสู้
}
```

## 🔧 การเชื่อมต่อระบบ

### 1. Auto-Connection
TurnManager จะหา Components อื่นๆ อัตโนมัติ:
- CombatManager
- TargetManager  
- PlayerController
- Player (GameObject ที่มี Tag "Player")

### 2. Event System
```csharp
// TurnManager Events
turnManager.OnCombatStarted += HandleCombatStarted;
turnManager.OnPlayerTurnStart += HandlePlayerTurn;
turnManager.OnEnemyTurnStart += HandleEnemyTurn;
turnManager.OnCombatEnded += HandleCombatEnded;
```

## 🎨 Visual Feedback

### 1. Gizmos (ใน Scene View)
- **วงกลมเหลือง**: ระยะเริ่มการต่อสู้ (2.0 หน่วย)
- **วงกลมแดง**: ระยะโจมตี (1.5 หน่วย)
- **เส้นเขียว**: เส้นทางไปยังเป้าหมาย

### 2. Debug Logs
```
🔄 TurnManager initialized successfully!
🏃 Moving to target: Enemy_Goblin
⚔️ Combat started! Enemy attacks first!
👹 Enemy's turn!
🗡️ Player's turn!
🏁 Combat ended! Player won!
🔄 Combat state reset
```

## ⚡ Performance Considerations

### 1. Coroutines
- ใช้ Coroutine สำหรับการวิ่งและการโจมตี
- หลีกเลี่ยงการใช้ Update() สำหรับการตรวจสอบทุก frame

### 2. State Management
- ใช้ Enum สำหรับสถานะการต่อสู้
- ตรวจสอบสถานะก่อนทำงานเพื่อป้องกันการทำงานซ้ำ

### 3. Component Caching
- เก็บ reference ของ components ไว้ในตัวแปร
- หา components ครั้งเดียวตอน Start()

## 🐛 การแก้ไขปัญหา

### 1. Player ไม่โจมตี
- ตรวจสอบว่า TurnManager ถูกสร้างและตั้งค่าถูกต้อง
- ตรวจสอบว่า CombatManager มี reference ถึง TurnManager
- ตรวจสอบว่า Player มี Tag "Player"

### 2. Enemy ไม่โจมตี
- ตรวจสอบว่า EnemyController มี reference ถึง TurnManager
- ตรวจสอบว่า PerformAttack() เป็น public method
- ตรวจสอบว่า Enemy มี PlayerStats component

### 3. การเคลื่อนที่ผิดปกติ
- ตรวจสอบระยะทางใน TurnManager
- ตรวจสอบว่า PlayerController ถูกปิด/เปิดอย่างถูกต้อง
- ตรวจสอบว่า Player มี Rigidbody2D

## 🔄 การทดสอบระบบ

### 1. Test Case 1: Normal Combat
1. สร้าง Enemy ในฉาก
2. Player คลิกขวาที่ Enemy
3. ตรวจสอบว่า Player วิ่งไปหา Enemy
4. ตรวจสอบว่า Enemy โจมตีก่อน
5. ตรวจสอบว่า Player โจมตีตาม

### 2. Test Case 2: Already in Range
1. วาง Player ใกล้ Enemy (≤ 1.5 หน่วย)
2. Player คลิกขวาที่ Enemy
3. ตรวจสอบว่าเริ่มการต่อสู้ทันที (ไม่วิ่ง)

### 3. Test Case 3: Combat End
1. เริ่มการต่อสู้
2. ทำให้ Enemy ตาย
3. ตรวจสอบว่าการต่อสู้จบและรีเซ็ตสถานะ

## 📝 Notes

- **Enemy โจมตีก่อนเสมอ** - นี่คือการออกแบบเพื่อให้เหมือนเกม Tibia แบบดั้งเดิม
- **Player โจมตีอัตโนมัติ** - ในรอบของ Player จะโจมตีอัตโนมัติ ไม่ต้องกดปุ่ม
- **ไม่มีการพลาด** - ระบบนี้ไม่มีการคำนวณโอกาสพลาดเพื่อความเรียบง่าย
- **Turn-based เท่านั้น** - ระหว่างการต่อสู้จะเป็น Turn-based อย่างสมบูรณ์

## 🎯 ถัดไป

ระบบ Turn-Based Combat นี้พร้อมใช้งานแล้ว! สามารถ:
- เพิ่ม Animation การโจมตี
- เพิ่ม Sound Effects
- เพิ่ม Combat Skills พิเศษ
- ปรับแต่งดีเลย์ระหว่างรอบ
- เพิ่ม UI แสดงสถานะการต่อสู้
